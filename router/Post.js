const express = require("express");const router = express.Router();const {createPost, getUserPosts, addLike, addComment, deletePost, updatePost} = require("../controllers/PostController");const AuthenticatedUser = require("../middleware/AuthenticatedUser");router.post('/create-post', AuthenticatedUser, createPost)router.get('/user-posts/:userId', AuthenticatedUser, getUserPosts)router.post('/like/:postId', AuthenticatedUser, addLike)router.post('/comment/:postId', AuthenticatedUser, addComment)router.delete('/delete-post/:postId', AuthenticatedUser, deletePost)router.put('/update-post/:postId', AuthenticatedUser, updatePost)module.exports = router;