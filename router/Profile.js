const express = require("express")const profile = express.Router()const {getAllUsers, getPersonalProfile, followUser, unfollowUser} = require("../controllers/ProfileController")const AuthenticatedUser = require("../middleware/AuthenticatedUser")profile.get('/all-users', AuthenticatedUser, getAllUsers)profile.get('/user', AuthenticatedUser, getPersonalProfile)profile.post('/follow/:userId', AuthenticatedUser, followUser)profile.post('/unfollow/:userId', AuthenticatedUser, unfollowUser)module.exports = profile;