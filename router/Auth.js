const express = require("express")const auth = express.Router()const {registerUser, resendVerificationToken, verifyUser, loginUser} = require("../controllers/authController");auth.post('/register', registerUser)auth.post('/verify-user', verifyUser)auth.post('/resend-verification-token', resendVerificationToken)auth.post('/login', loginUser)module.exports = auth;